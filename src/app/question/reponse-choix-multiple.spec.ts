import { ReponseChoixMultiple } from './reponse-choix-multiple';

describe('ReponseChoixMultiple', () => {
  it('should create an instance', () => {
    expect(new ReponseChoixMultiple()).toBeTruthy();
  });
});
